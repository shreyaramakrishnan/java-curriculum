package arrays;
import java.util.*;
public class SwapAll {
    public static void main(String[] args) {
        int[] a1 = {11, 42, -5, 27, 0, 89};
        int[] a2 = {10, 20, 30, 40, 50, 60};
        swapAll(a1, a2);
        System.out.println(Arrays.toString(a1));
        System.out.println(Arrays.toString(a2));
    }

    /* 
     * Write a static method named swapAll that accepts 
     * two arrays of integers as parameters and swaps their 
     * entire contents. You may assume that the arrays passed
     *  are not null and are the same length.
     */
    public static void swapAll(int[] arr) {
        // TODO: Your code here
    }
}