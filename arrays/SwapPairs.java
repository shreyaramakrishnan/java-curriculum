package arrays;

import java.util.*;
public class SwapPairs {
    public static void main(String[] args) {
        String[] arr1 = {"four", "score", "and", "seven", "years", "ago"};
        swapPairs(arr1);
        System.out.println(Arrays.toString(arr1));
        
        String[] arr2 = {"to", "be", "or", "not", "to", "be", "hamlet"};
        swapPairs(arr2);
        System.out.println(Arrays.toString(arr2));
    }

    /* 
     * Write a static method named swapPairs that accepts an array of 
     * strings as a parameter and switches the order of values in a 
     * pairwise fashion. Your method should switch the order of the first
     *  two values, then switch the order of the next two, switch the
     *  order of the next two, and so on. 
     */

    public static void swapPairs(String[] arr) { 
    }
}